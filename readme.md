## 🚀 Start the Development Server (📁 **pw-practice-app**)

Navigate to the server folder:

**Path:** `./pw-practice-app`

```bash
cd pw-practice-app
npm install --force
npm start
```

The Angular app will be available at:
👉 **[http://localhost:4200](http://localhost:4200)**

Navigate to the **pw-practice-app** folder and run the following commands:

```bash
cd pw-practice-app
npm install --force
npm start
```

The Angular app will be available at:
👉 **[http://localhost:4200](http://localhost:4200)**

To start the Angular development server, run the following commands:

```bash
npm install --force
npm start
```

Once the server starts, the app will be available at:
👉 **[http://localhost:4200](http://localhost:4200)**

---

## ⚡ Setup (📁 **test-automation**)

Navigate to the Playwright framework folder:

**Path:** `./test-automation`

```bash
cd test-automation
npm install --force
npm start
```

Navigate to the **test-automation** folder and run:

```bash
cd test-automation
npm install --force
npm start
```

Navigate to the `test-automation` folder and run:

```bash
cd test-automation
npm install --force
npm start
```

---

## 📝 Notes

* Ensure **Node.js** and **npm** are installed on your system.
* Use `npm install --force` to resolve dependency issues.

```bash
cd test-automation
npm install
npm start
```

---

## 📝 Notes

* Ensure **Node.js** and **npm** are installed on your system.
* If you encounter dependency conflicts, use:

```bash
npm install --force
```
